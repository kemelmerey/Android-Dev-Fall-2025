<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for LIGHT mode. -->
    <!-- The name is changed to "Base.Theme.Instagram_SIS1" -->
    <style name="Base.Theme.Instagram_SIS1" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- The 'true' value makes status bar icons (like time and battery) dark. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:listDivider">@color/light_mode_divider</item>
    </style>

    <!-- This is the actual theme applied to your app. It correctly inherits from the Base theme. -->
    <style name="Theme.Instagram_SIS1" parent="Base.Theme.Instagram_SIS1" />
</resources>