<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for DARK mode. -->
    <style name="Base.Theme.NewsApp_SIS2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- The 'false' value makes status bar icons (like time and battery) light. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Customize your dark theme here. -->
        <item name="android:colorBackground">@color/dark_gray</item> <!-- Assuming you have a dark_gray color defined -->
        <item name="colorSurface">@color/dark_gray</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:listDivider">@color/dark_mode_divider</item>
    </style>

    <style name="Theme.NewsApp_SIS2" parent="Base.Theme.NewsApp_SIS2" />
</resources>